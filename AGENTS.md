# Project agent instructions

- Follow the implementation plan in `docs/implementation-plan.md` and keep it
  synchronized with delivered behavior.
- For every implementation, test, or documentation step, append a dated entry
  to `workspace/notes/Dev/MainDevNotes/DevProjects/DiagramsCLI/Task - Implement
  diagrams-cli JSON-to-diagram pipeline.md`.
- Each work-log entry must summarize changes, validation commands and results,
  and relevant commit hashes. Update the task note's subtask checkboxes as work
  is completed.
