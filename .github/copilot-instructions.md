# GitHub Copilot Instructions

- Keep changes focused and small; prefer incremental improvements.
- Maintain compatibility with Python 3.9+.
- Use `src/` layout for Python packages and update `pyproject.toml` accordingly.
- Favor argparse for CLI parsing until a richer UX is required.
