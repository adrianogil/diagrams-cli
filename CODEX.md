# Codex Instructions

- Repository uses a `src/` layout for Python packages.
- CLI entrypoint lives at `diagrams_cli.cli:main`.
- Keep placeholder behavior explicit until generator logic is implemented.
- Prefer updating docs and scaffolding alongside new CLI features.
